# Mining service memory settings.
#
# Copy this file to .env and adjust the values for the host running Docker.
# Docker Compose reads .env automatically for variable substitution.

# Hard memory limit for the Java mining container.
PROM_SERVICE_MEM_LIMIT=28g

# JVM heap sizing inside the mining container. Keep Xmx below
# PROM_SERVICE_MEM_LIMIT to leave memory for native allocations, threads,
# metaspace, the OS, and container overhead.
PROM_JAVA_XMS=4g
PROM_JAVA_XMX=26g
